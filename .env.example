# Copy this file to .env and fill in real values.
# .env is in .gitignore — never commit it.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=sih26146
DB_USER=sih_user
DB_PASSWORD=changeme

API_HOST=0.0.0.0
API_PORT=8000
DEBUG=false

# Set to true during hackathon to use local parquet files instead of BigQuery
OFFLINE_MODE=true
